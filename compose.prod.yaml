services:
  web:
    image: carthographie/lyrics-slide-show:latest
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - .env.prod
    ports:
      - "${LSS_BIND_PORT:-8000}:8000"
    restart: unless-stopped
